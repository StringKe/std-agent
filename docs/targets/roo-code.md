# Target: Roo Code（已移除）

Roo Code 官方扩展已于 2026-05-15 关停，GitHub 仓库归档。README 指向社区 fork ZooCode 与 Cline，二者都不是同一套 `.roo/` 配置面。

- https://github.com/RooCodeInc/Roo-Code

stdagent 自本次起从 `ValidTargets` 删除 `roo-code`。旧 `config.toml` 若仍启用该名会校验失败。需要替代时使用 `cline`。
